<UserControl x:Class="Editor.GameOptionsEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="400">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PanelBackground" Color="#2C313A"/>
        <SolidColorBrush x:Key="TextBrush" Color="#F8F8F2"/>
        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#E53935"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B71C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Border Background="{StaticResource PanelBackground}" CornerRadius="6" Padding="20">
        <StackPanel>
            <TextBlock Text="Game Options" FontWeight="Bold" FontSize="18" Margin="0,0,0,10" Foreground="{StaticResource TextBrush}"/>
            <TextBlock Text="Default Room:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox x:Name="DefaultRoomComboBox" Margin="0,0,0,10" SelectionChanged="DefaultRoomComboBox_SelectionChanged"/>
            <TextBlock Text="Game Title:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="GameTitleTextBox" Margin="0,0,0,10"/>
            <TextBlock Text="Window Width:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="WindowWidthTextBox" Text="800" Margin="0,0,0,5"/>
            <TextBlock Text="Window Height:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="WindowHeightTextBox" Text="600" Margin="0,0,0,10"/>
            <Button Content="Save Game Options" Click="SaveGameOptions_Click" Padding="8,2"/>
        </StackPanel>
    </Border>
</UserControl> 